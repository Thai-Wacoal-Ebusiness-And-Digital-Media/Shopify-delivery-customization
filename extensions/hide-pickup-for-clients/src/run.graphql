query RunInput {
  cart {
    buyerIdentity {
      customer {
        hasAnyTag(tags: ["Client_NoPickup"])
      }
    }
    deliveryGroups {
      deliveryOptions {
        handle
        title
        deliveryMethodType
      }
    }
  }
}
